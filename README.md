
//jab system naya ho tab--- aur system name aur email maange github access ke liye ---- 
        extra command -- (
          git config --global user.email "vy333670@gmail.com"
          git config --global user.name "Vivek-yadav33"
        )

//new repository(repo) 
//create a repo on github
//open terminal in folder 
step 1: git init
step 2: git remote add origin (paste the link)
step 3: git add .
step 4: git status
step 5: git commit -m "write your message here"
step 6: git push origin master

//jab system pe file/code ho aur pehle se hi git init kiya gaya hai ho tab
repeat from step 3 to step 6 ( from line 6 ) 

//for new system clone the code (repo)
step 1:git clone (paste the link)
repeat from step 3 to step 6 ( from line 6 ) 
